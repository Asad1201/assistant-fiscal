venv/
.env
__pycache__/
*.pyc
*.pyo
.DS_Store
documents/cgi/*.pdf
documents/lois-finances/*.pdf
# index/ est versionne : il est necessaire au deploiement (Streamlit Cloud n'a
# pas les PDF sources pour le reconstruire). Le rebuild + commit = redeploiement.
